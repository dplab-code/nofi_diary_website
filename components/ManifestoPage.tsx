import Link from "next/link";
import { SiteFooter, SiteHeader } from "@/components/SiteChrome";
import { copy, Locale, localePath, manifesto } from "@/lib/i18n";
export function ManifestoPage({ locale }: { locale: Locale }) { const t = copy(locale); const content = manifesto(locale); return <main className="manifestoPage"><SiteHeader locale={locale} nav={t.nav} manifesto /><header className="manifestoHero shell"><p className="kicker">NoFi Diary</p><h1>{content.title}</h1><p>{content.blocks[0].join(" ")}</p></header><article className="manifestoBody shell" aria-label={content.title}>{content.blocks.slice(1).map((lines, i) => <section key={i} className={i % 4 === 0 ? "manifestoEmphasis" : ""}>{lines.map((line, j) => <p key={j}>{line}</p>)}</section>)}<footer className="manifestoEnd"><span>♡</span><p>NoFi Diary</p><Link className="textLink" href={localePath(locale)}>← Back to NoFi</Link></footer></article><SiteFooter locale={locale} text={t.footer} nav={t.nav} /></main>; }
